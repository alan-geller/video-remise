<Page
    x:Class="FencingReplay.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FencingReplay"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid x:Name="LayoutGrid">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <!--<ColumnDefinition Width="*" />-->
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <!--<ListBox x:Name="ListBox1" Width="600" Height="100" Grid.Column="0" Grid.Row="0" SelectionMode="Single"
                  SelectionChanged="ListBox1_OnSelectionChanged"/>
        <CaptureElement x:Name="Capture1" Grid.Column="0" Grid.Row="1"/>-->
        <!--<ListBox x:Name="ListBox2" Width="600" Height="100" Grid.Column="1" Grid.Row="0" SelectionMode="Single" 
                  SelectionChanged="ListBox2_OnSelectionChanged"/>
        <CaptureElement x:Name="Capture2" Grid.Column="1" Grid.Row="1"/>-->
    </Grid>
</Page>
